pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
// Change here to update Root project name in AS or IntelliJ
rootProject.name = "ScratchTemplate"
include (":app")
